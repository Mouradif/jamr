import styled from 'styled-components';

const LogoIcon = styled.span`
  align-items: center;
  display: inline-flex;
  
  svg {
    font-size: 45px;
    margin-right: 8px;
  }

  h5 {
    font-weight: 900;
  }
`;

const Styled = { LogoIcon };

export default Styled;